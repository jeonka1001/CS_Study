# 면접준비 - WEB
> 나만의 언어로 준비하는 면접준비

## 목록

---

## http vs https
> hypertext transfer protocal (secure)
> 웹 상에서 클라이언트와 서버 간에 요청/응답 으로 정보를 주고 받을 수 있는 프로토콜  
- 기존 http 의 경우 서버 - 브라우저 전송 데이터가 암호화 되지 않음.
- 이를 방지하기위해 https 의 경우 ssl(socket secure layer)인증서를 사용함.

#### http 포로토콜 구조 - request message
- start line
    - 요청 대상을 가리키는 한 줄의 문자열  
    - http method / request uri / http version  
- header
    - 요정에 대한 부가 설명
    - 일반 헤더 : http 본문 컨텐츠와 관련 없이 hhtp 통신에 대한 일반적인 정보로 요청, 응답 모두 공통 ( 요청 시간, 요청 생성 날짜 등 )
    
    - 요청 헤더 : 사용자에 관련된 정보가 들어있다. ( 요청 브라우저, 사용자의 운영체제 등 )
    - 엔티티 헤더( 보내는 데이터 정보 POST 일 때만 보낸다 ) 바디 내용이 존재할 때 이에 대한 세부 정보 설명
- blank line
    - 헤더와 바디를 구분하기 위한 공백 줄
- body 
    - 엔티티 헤더에 들어있는 실제 데이터가 들어있다.

#### http method
> GET POST HEAD DELETE
- 아래 메서드를 용도와 다르게 사용할 수 있지만, 누구나 해당 메소드를 보고 어떤 동작인지 유추할 수 있도록 해당 메소드에 행위를 용도에 맞게 사용하는 것이 좋다.

###### GET vs POST
||GET|POST|
|--|--|--|
|캐싱|가능|불가능|
|요청길이제한|있다|없다|
|보안|취약|안전|
|기록|남는다|남지않는다|
- GET 의 경우 서버에 리소스 조회를 요청할 시 사용하며, 요청 시 파라미터를 url 에 담아 서버로 보낸다
- POST 의 경우 서버에 리소스 추가, 수정을 요청할 시 사용되며, 요청 시 파라미터를 body 에 담아 서버로 보낸다.

###### HEAD 
- 요청 시 응답 헤더만 요청하며 , 응답 바디는 받지 않는다.
    - 주로 해당 페이지가 유효한지 알아보기 위해 사용

###### DELETE
- 데이터 삭제 요청시 사용하는 http method

---

## session vs cookie
> 웹 통신을 하며 유지하려는 정보( 로그인 정보 등 )를 저장하기 위해 사용하는 것
- 클라이언트와 서버가 통신을 해면 통신이 연속적으로 이루어지지 않고, 한번 통신시 끊어집니다. 그래서 이 정보를 유지하기 위해 사용합니다.

- 저장위치, 용량제한, 만료시점의 차이가 있음  

||Session|Cookie|
|--|--|--|
|저장 위치|서버의 DB|사용자 개인 PC|
|만료 시점|브라우저 종료시|저장 시 설정|
|상대적인 속도|느림|빠름|
|상대적인 보안|좋음|안좋음|
|용량|서버 상태에 따름|총300개(개당4KB)|

- 세션은 서버 자원을 사용하며, 보안이 좋기 때문에 보안에 민감함 사용자 정보를 세션으로 저장하며, 특정 자원 접근 시 사용자 판별(?) 을 위해 사용한다.
- 쿠키는 보안은 좋지 않으나 서버 자원을 활용하지 않기 때문에 보안에 민감하지 않는 정보를 캐싱해놓기 위해 사용한다.

- 쿠키 : 아이디 비밀번호를 저장하시겠 습니까?
- 세션 : 로그인 구분 등 보안상 중요한 작업

---

## RESTful 하다
- 각 구성 요소의 역할이 분리되어 설계 의도를 명확히 하는 것
- URI 의 명확한 정의
#### REST (REpresentational State Treansfer)
- 대표적인 상태 전환
- 원하는 자원을 Http URI 를 통해 명시하고 Http Method 를 통해 자원에 대한 CRUD 를 선택적이게 적용하는 것

#### 사용이유 ?
- 일관된 인터페이스
- 클라이언트 서버 분리
- 자체 표현( 자원과 행위로 의도를 분명하게 한다 )
#### URI ?
- 자원을 구조와 함께 나타내는 것

---

## MVC1 vs MVC2
- MVC1
    - JSP 에서 클라이언트 요청을 받아 직접 처리하고 이를 출력해준다.
    - 장점 : 구조가 단순하다
    - 단점 : JSP 내에 html 코드와 java 코드가 같이 있어 한눈에 보기 복잡하고 유지보수가 어렵다
- MVC2
    - JSP 는 단순 출력만 담당하고 서블릿이 비즈니스 로직과 흐름을 제어해준다.
    - 장점 : 유지보수 하기에 좋다
    - 단점 : 구조가 어렵다.
    

---
## http 상태 코드
#### 200
- 정상작동
#### 400 
- 클라이언트 오류 
- 요청 문법, 잘못된 요청, 요청 처리 불가
#### 500 
- 서버 오류
- 요청은 유효하나, 수행하지 못함

---

## pulling polling

#### polling
- 서버의 결과를 주기적으로 요청하는 것
- 클라이언트의 요청에 해당하는 쿼리를 서버에서 작성해서 데이터베이스에서 해당 결과를 반환받아 클라이언트에게 주는 것


#### pulling 
- 서버의 데이터를 주기적으로 가져가는 것
- 클라이언트에서 직접 쿼리를 작성해서 데이터베이스에서 정보를 가져가는 것

---

## TCP vs UDP

### TCP
- 연결 지향형 프로토콜
- 사강 회선을 만들어 신뢰성 보장
- UDP 에 비해 속도가 느리다.
- 파일전송 등

### UDP
- 데이터를 데이터그램 단위로 전송하는 프로토콜
- 신뢰성을 보장하지는 않는다.
- 속도( 연속성 )가 더 중요한 통신의 경우 사용
- 스트리밍 등

---

## 3,4 way handshake
#### 3 way handshake 
- TCP 프로토콜을 이용해서 정확한 데이터 전송이 가능한지 확인하는 과정
- 연결 전 사용
1. 클라이언트가 서버 접속 요청하는 패킷을 보낸후 대기
2. 서버가 패킷을 받았다면 접속 수락 패킷을 보낸 후 클라이언트 응답을 대기한다.
3. 클라이언트가 패킷을 받은후 서버에게 접속 전 패킷을 보낸후 서버에 접속해 데이터를 전송한다.

#### 4 way handshake
- 연결 세션을 종료하기 위해 수행되는 절차
1. 클라이언트가 연결 종료 패킷 전송
2. 서버는 메시지 수신 확인 패킷을 보낸 후 본인 통신이 끝날때까지 time wait 으로 기다린다.
3. 서버 통신이 끝낫다면 연결이 종료되었다고 클라이언트에게 종료 패킷을 보낸다.
4. 클라이언트는 확인 패킷을 보낸다

---

## OSI 7계층
- 네트워크를 구성하는 요소를 7계층으로 표현할 것 입니다.
- 문제가 발생 시 어느 계층에서 문제가 발생한지 파악할 수 있기 때문에 해결하기 용이해집니다.

- 물리, 데이터링크, 네트워크, 전송, 세션, 표현, 응용 계층

#### 응용 계층 ( Application Layer )
- 최상위 계층
- 사용자에게 보이는 유일한 계층 ( 사용자를 위한 인터페이스 지원 )
- 예 ) 메일, 인터넷, HTTP 등

#### 표현 계층 ( Presentation Layer )
- 응용 계층에서 전달받은 데이터를 인코딩하고, 암호화 하 후 세션계층에 전달.
- 세션 계층에서 전달받은 데이터를 디코딩하고, 복호화 한 후 응용계층에 전달하기도 한다.
- 암호화/복호화, 인코딩/디코딩 담당

#### 세션 계층 ( Session Layer )
- 네트워크상 양쪽 연결을 관리 및 지속 
- 네트워크로 연결된 양쪽 응용 프로세스가 통신을 관리하는 방법 제공

#### 전송 계층 ( Transport Layer )
- 데이터 전송 및 전송 속도 조절 ( 흐름 처리 )
- 전송 중 발생한 오류 처리( 재전송 전송취소 등 )
- 포트 번호를 통해 목적 컴퓨터의 목적 **프로세스** 로 전달 (프로세스 선정)
- UDP/TCP ( 신뢰성/비신뢰성 )

#### 네트워크 계층 ( Network Layer )
- 데이터를 목적지까지 경로를 찾아 전송하는 역할
- 목적지의 주소(IP)를 통해 데이터가 전송될 경로(Router) 를 결정
- Router : IP 주소 , Switch : MAC 주소  
    - IP 주소를 통해 도착지까지 이동하며, 해당 IP를 사용하는 여러 기기 중 특정 기기를 구별할 때 MAC 주소를 사용한다.  
*라우팅기법? 나중에 알아보기*

#### 데이터 링크 계층( DataLink Layer )
- 네트워크 계층에서 특정 IP 까지 도착한 데이터를 MAC 주소를 이용해 특정 기기로 전달한다.
- 데이터를 전달 할 노드 선택

#### 물리 계층 ( Physical Layer )
- 비트 단위의 data 를 물리적으로 , 물리적인 장치로 전송하는 계층 ( 전기신호를 주고받는 것이 주 목적이다. )

----

## TCP/IP 4계층
- OSI 는 시스템 연결을 위한 모델이라면 TCP/IP는 웹서비스에 맞게 단순화 시킨 모델
- Application(7-5), Transport(4), Internet(3), NetworkInterface(2-1) 로 총 4개의 계층으로 나눠진다.

#### Application Layer
> 데이터 단위 : Data/Message
- 기존 OSI 7계층의 응용, 표현, 세션 계층에 해당된다.
- TCP/UDP 기반 응용 프로그램 구현시 사용 ( 대표적으로 Http의 경우 TCP 방식으로 통신한다.)
- 파일전송, 이메일, DNS, HTTP, FTP SSH 등

#### Transport Layer
> 데이터 단위 : Segment, 전송주소 : Port  
- 기존 OSI 의 Transport 와 같은 역할을 한다.
- 통신 노드 연결 흐름 제어 및 오류 발생시 제어
- TCP/UDP

#### Internet Layer
> 데이터 단위 : 패킷, 전송주소 : IP  
- 기존 OSI 의 NetWork Layer에 해당한다.
- 데이터 정의, 데이터 경로 라우팅 ( 논리 주소(IP) 할당 )
- 오류 발생에 대한 대비가 되어있지 않음
- IP 프로토콜

#### Network Interface
> 데이터 단위 : 프레임, 전송주소 : MAC  
- 기존 OSI 의 Physical, DataLink 에 해당한다.
- 데이터가 네트워크를 통해 어느 물리적인 곳에 전송되는지 정의 ( 물리주소(MAC) 할당 )
- LAN/WAN, 패킷망 등
